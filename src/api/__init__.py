"""
HTTP API package for real-time inference.

**Module:** ``src/api/api.py`` defines the FastAPI ``app``. Import ``PatientData`` for
typing/tests; run the server via ``python src/api/api.py`` or uvicorn (see module docstring).

**Author.** SANDIP BHATTACHARYYA — BITS Pilani ID 2025cs05025
"""
